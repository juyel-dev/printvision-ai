// Content Map Types - Frozen Contract
// Per CONTENT_MAP_SCHEMA.md

#[derive(Debug, Clone, PartialEq)]
pub struct ContentMap {
    pub schema_version: u32,
    // ... other fields per schema (stub for validator)
}

// Enums for classifications etc. (expand from M3)